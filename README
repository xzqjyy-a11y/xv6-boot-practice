## 一、实验内容说明

本次实验基于 xv6 操作系统源码，通过实际编译、运行与代码修改，完成了以下任务：

### 第一层任务

- **系统调用路径跟踪（write）**
  - 在用户层、系统调用分发层以及内核实现层添加调试输出
  - 观察 `echo` 命令从用户态进入内核态的完整执行路径

- **调度器行为观察**
  - 在 `scheduler()` 中添加日志输出
  - 分析进程在 CPU 上的调度与切换过程

- **内存分配过程观察**
  - 在 `kalloc()` 中打印分配的物理页地址
  - 理解 xv6 的物理内存管理机制

### 第二层任务

- **实现新的系统调用 `getpid_plus`**
  - 在内核中新增系统调用函数
  - 注册到 syscall 表
  - 添加用户态接口并编写测试程序
  - 验证返回值为 `pid + 1`

---

通过以上任务，逐步理解了 xv6 在系统调用、进程调度以及内存管理等方面的基本实现原理。

## 二、第一层与第二层任务的实现过程

### 一、第一层任务实现过程

#### 1. 系统调用路径跟踪（write）

**实现思路：**  
通过在系统调用路径的关键位置插入调试输出，观察 `echo` 命令从用户态进入内核态再返回的全过程。

**具体步骤：**

1）用户层（`echo.c`）  
在调用 `printf` 前添加提示信息：

```c
printf(1, "[USER] calling write\n");
```

2）系统调用分发（`syscall.c`）  
在 `syscall()` 中判断 `SYS_write` 并打印：

```c
if(num == SYS_write)
  cprintf("[KERNEL] enter syscall\n");
```

3）系统调用实现（`sysfile.c`）  
在 `sys_write()` 中添加：

```c
cprintf("[KERNEL] sys_write invoked\n");
```

**运行结果：**

```text
$ echo hello
[USER] calling write
[KERNEL] enter syscall
[KERNEL] sys_write invoked
hello
```

**结论：**  
验证了系统调用路径：

```text
用户态 → 中断 → syscall 分发 → sys_write → 返回用户态
```

---

#### 2. 调度器行为观察

**实现思路：**  
在进程调度函数中输出当前被调度的进程 pid。

**修改位置：** `proc.c` 中 `scheduler()`：

```c
cprintf("[SCHED] switch to pid=%d\n", p->pid);
```

**运行结果：**

```text
[SCHED] switch to pid=1
[SCHED] switch to pid=2
[SCHED] switch to pid=2
...
```

**结论：**

- xv6 采用轮转调度策略
- `scheduler()` 持续循环扫描 `RUNNABLE` 进程

---

#### 3. 内存分配过程观察

**实现思路：**  
在物理内存分配函数中输出每次分配的地址。

**修改位置：** `kalloc.c`

```c
if(r)
  cprintf("[MEM] alloc page at %p\n", r);
```

**运行结果：**

```text
[MEM] alloc page at 0x8df33000
[MEM] alloc page at 0x8df32000
...
```

**结论：**

- 每次分配一个 4KB 页
- 地址递减，说明使用空闲链表分配

---

### 二、第二层任务实现过程

#### 实现新的系统调用：getpid_plus

**功能说明：**  
返回当前进程的 `pid + 1`。

---

#### 实现步骤

1）定义系统调用号（`syscall.h`）

```c
#define SYS_getpid_plus 23
```

2）声明函数（`defs.h`）

```c
int sys_getpid_plus(void);
```

3）实现内核函数（`sysproc.c`）

```c
int
sys_getpid_plus(void)
{
  return proc->pid + 1;
}
```

4）注册系统调用（`syscall.c`）

```c
extern int sys_getpid_plus(void);

[SYS_getpid_plus] sys_getpid_plus,
```

5）用户态接口（`user.h`）

```c
int getpid_plus(void);
```

6）系统调用入口（`usys.S`）

```asm
SYSCALL(getpid_plus)
```

7）编写用户程序（`getpidplus.c`）

```c
#include "types.h"
#include "stat.h"
#include "user.h"

int
main(void)
{
  printf(1, "pid = %d\n", getpid());
  printf(1, "getpid_plus = %d\n", getpid_plus());
  exit();
}
```

8）修改 `Makefile`

```makefile
UPROGS=\
  _getpidplus\
```

**运行结果：**

```text
$ getpidplus
pid = 3
getpid_plus = 4
```
![getpid_plus 系统调用运行截图](getpidplus.png)

**结论：**  
成功实现新的系统调用 `getpid_plus`，并完成从用户态到内核态的完整调用流程。

## 三、遇到的问题及解决方法

### 问题1：make 无法识别命令

- **原因**：在错误目录执行
- **解决**：进入包含 `Makefile` 的目录再执行 `make`

---

### 问题2：系统调用输出混乱

- **原因**：`write` 系统调用是按字符触发的，每个字符都会进入内核
- **解决**：
  - 只对 `SYS_write` 做条件打印
  - 或减少打印内容

---

### 问题3：调度日志刷屏

- **原因**：`scheduler()` 是死循环，不断调度进程
- **解决**：
  - 限制打印次数（例如加计数器）
  - 或仅在特定条件下打印

---

### 问题4：VSCode 报红但可编译

- **原因**：VSCode 的语法分析器不完全支持 xv6 内核代码
- **解决**：以 `make` 编译结果为准，不影响实际运行

## 四、个人总结

通过本次 xv6 实验，我对操作系统底层运行机制有了更加深入且直观的理解。起初，我仅通过理论学习了解操作系统的启动流程、系统调用以及进程调度等概念，但在实际阅读 xv6 源码并进行动手修改之后，这些原本抽象的内容逐渐变得具体且清晰。

在启动流程部分，我从 BIOS 加载 bootloader 开始，逐步跟踪到内核 `main` 函数的执行过程，完整理解了 CPU 从 **实模式（Real Mode）切换到保护模式（Protected Mode）**，再到开启分页机制（Paging）的全过程。这一过程让我第一次真正理解了操作系统是如何从无到有被加载并运行起来的。

在系统调用路径跟踪实验中，我清晰地观察到用户态程序如何通过中断机制进入内核态，以及内核如何根据系统调用号分发并执行对应的函数。这一过程加深了我对**用户态与内核态切换机制**的理解。

在调度器实验中，通过在 `scheduler()` 中加入调试输出，我观察到了进程在 CPU 上的切换行为，也验证了 xv6 使用的是**基于时间片的轮转调度（Round-Robin）策略**。

在内存管理实验中，通过在 `kalloc()` 中打印分配地址，我直观地看到物理页的分配过程，理解了 xv6 如何通过空闲链表（freelist）管理内存资源。

此外，在实现 `getpid_plus` 系统调用的过程中，我完整经历了一次从内核扩展到用户态接口的开发流程，包括：
- 系统调用号定义
- 内核函数实现
- syscall 表注册
- 用户态封装与调用

这一过程让我对操作系统的模块结构有了更加系统性的认识。

**总体而言，本次实验不仅提升了我阅读和理解底层系统代码的能力，也让我对操作系统的整体运行机制建立了清晰的认知，是一次非常有价值的实践经验。**
