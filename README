# xv6 启动流程实践（rev9）

## 一、项目说明

本项目基于 MIT xv6 操作系统（x86，rev9 版本），通过实际编译、运行和源码阅读的方式，分析并理解 xv6 的完整启动流程。  
实验内容包括：  
- 成功在 QEMU 中启动 xv6  
- 阅读并注释启动相关源码  
- 通过 VGA 文本输出可视化启动过程  
- 使用 Git 进行分阶段版本管理  

---

## 二、启动流程说明

xv6 的启动过程可以分为 **Bootloader 阶段** 和 **Kernel 阶段**，整体流程如下：

### 1. BIOS → bootblock

- 计算机上电后，BIOS 会将磁盘的第一个扇区（512 字节）加载到内存物理地址 `0x7C00`
- 该扇区对应 xv6 的 **bootblock**，由 `bootasm.S` 和 `bootmain.c` 共同组成
- CPU 以 **16 位实模式** 开始执行

### 2. bootasm.S（实模式 → 保护模式）

`bootasm.S` 主要完成以下工作：

- 关闭中断（`cli`）
- 初始化段寄存器
- 打开 A20 地址线，突破 1MB 内存限制
- 加载临时 GDT
- 将 CPU 从 **16 位实模式** 切换到 **32 位保护模式**
- 设置栈指针后，调用 `bootmain()`

### 3. bootmain.c（加载内核）

`bootmain.c` 的主要功能是加载内核 ELF 文件：

- 从磁盘读取内核 ELF 头部
- 校验 ELF magic number
- 遍历 program header
- 将内核各段加载到 ELF 指定的物理地址
- 对 BSS 段进行清零
- 跳转到 ELF 中指定的内核入口地址

### 4. entry.S（进入内核）

`entry.S` 是内核的入口文件，主要完成：

- 启用 4MB 大页支持
- 加载内核页目录
- 打开分页机制（Paging）
- 设置内核栈
- 跳转到 `main()`，正式进入内核 C 代码

### 5. main.c（内核初始化）

`main()` 函数中依次完成：

- CPU 与中断初始化
- 内存管理初始化
- 进程表与调度器初始化
- 文件系统初始化
- 创建第一个用户进程 `init`
- 启动 shell，系统进入可交互状态

---

## 三、运行方法

### 实验环境
- 操作系统：Ubuntu / WSL
- 编译器：gcc（32-bit 支持）
- 虚拟机：QEMU

### 编译与运行
在 xv6 项目目录下执行：

```bash
make clean
make qemu
---

## 四、运行结果

![xv6 启动过程截图](boot.png)


在 WSL + QEMU 环境下成功编译并运行 xv6，系统能够正常完成启动流程并进入 shell。

运行 `make qemu` 后，可以在 QEMU 虚拟机窗口中观察到以下结果：

- 内核启动信息正常输出，例如：
  - `cpu0: starting xv6`
  - `init: starting sh`
- 系统进入 shell，出现 `$` 提示符
- 可以正常执行 `ls`、`echo` 等基本命令

在 Task3 中，为了更直观地观察启动过程，尝试在 bootloader 阶段通过向 VGA 文本缓冲区（物理地址 `0xB8000`）写入字符的方式，输出 `[BOOT]` 相关信息，用于标识内核加载前后的关键阶段。

由于后续内核初始化及控制台输出会覆盖屏幕内容，启动阶段的 VGA 文本信息不会长期保留，但 xv6 能够正常启动并运行，说明启动流程实现正确。
---

## 五、个人总结

通过本次 xv6 启动流程实践，我对操作系统从上电到进入用户态运行的完整过程有了更加直观和深入的理解。

在阅读并注释 `bootasm.S`、`bootmain.c` 和 `entry.S` 的过程中，我实际看到了 CPU 从 16 位实模式切换到 32 位保护模式、内核 ELF 文件加载以及分页机制开启等关键步骤，这些内容不再只是课本中的抽象概念。

同时，在实现启动阶段 VGA 文本输出的过程中，我也体会到 bootloader 阶段在代码体积和执行环境上的严格限制，例如 boot block 必须控制在 512 字节以内，这让我对操作系统底层设计的严谨性有了更深的认识。

整体而言，本次实验不仅加深了我对 xv6 启动流程的理解，也提高了我阅读和分析底层系统代码的能力，为后续操作系统课程的学习打下了基础。

